import 'package:flutter/material.dart';

class SelectionInputWidget extends StatelessWidget {
  const SelectionInputWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}